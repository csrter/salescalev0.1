version: "3.8"

services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: salescale
      POSTGRES_PASSWORD: salescale
      POSTGRES_DB: salescale
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
